[pytest]
testpaths = tests
norecursedirs = community_contributions frontend node_modules .venv venv __pycache__ .git
# Test auth bypass: allows X-Dev-Tenant fallback in tests (uses get_current_user fallback)
# Note: This is safe for pytest only; production requires JWT
env =
    TEST_AUTH_BYPASS=true
