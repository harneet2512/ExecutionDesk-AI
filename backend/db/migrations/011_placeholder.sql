-- Migration 011: Placeholder
-- This migration number was skipped during development.
-- No schema changes are required.  The file exists to document the gap
-- in the migration sequence (010 -> 012) and prevent confusion.
SELECT 1;
